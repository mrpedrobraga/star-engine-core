[plugin]

name="Max Size Container"
description="This is a custom container that limits it's child maximum size.

1. Add a Control node as a child
2. Set it's maximum width and height
3. Choose how to align the child node when themaximum size is reached

Note that the container can only accept 1 child."
author="Matthieu Huvé"
version="1.0"
script="plugin.gd"
